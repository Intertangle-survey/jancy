// msvc15 x86 release crashes if exception is thrown from LLVM JIT code (ESP gets shifted by 4 bytes)

destruct()
{
	printf("destruct()\n");
	assert(false);
}

int main()
{
	printf("main()\n");
	assert(false);
	return -1;
}